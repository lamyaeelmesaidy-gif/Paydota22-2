name: Build Android APK

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build-android:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'
        
    - name: Setup Java
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '21'
        
    - name: Install dependencies
      run: npm ci
      
    - name: Build web assets
      run: npm run build
      
    - name: Copy keystore file
      run: cp attached_assets/signing_1750726650743.keystore android/app/signing.keystore
      
    - name: Sync Capacitor
      run: npx cap sync android
      
    - name: Build Android APK (Debug)
      working-directory: android
      run: ./gradlew assembleDebug
      
    - name: Build Android APK (Release)
      working-directory: android
      run: ./gradlew assembleRelease
      
    - name: Upload Debug APK
      uses: actions/upload-artifact@v4
      with:
        name: paydota-debug-apk
        path: android/app/build/outputs/apk/debug/app-debug.apk
        
    - name: Upload Release APK
      uses: actions/upload-artifact@v4
      with:
        name: paydota-release-apk
        path: android/app/build/outputs/apk/release/app-release.apk
        
    - name: Create Release with APK
      if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
      uses: ncipollo/release-action@v1
      with:
        tag: v${{ github.run_number }}
        name: PayDota Mobile v${{ github.run_number }}
        body: |
          🎉 **تطبيق PayDota المحمول - إصدار تلقائي**
          
          📱 **المميزات:**
          - ✅ APK موقع جاهز للتحميل والتثبيت
          - ✅ جميع الميزات المحمولة مفعلة
          - ✅ keystore للتوقيع الآمن
          - ✅ تم البناء والاختبار تلقائياً
          
          📥 **التثبيت:**
          قم بتحميل ملف APK أدناه وتثبيته على جهاز Android
          
          🏗️ **Build #${{ github.run_number }}**
        artifacts: android/app/build/outputs/apk/release/app-release.apk
        artifactContentType: application/vnd.android.package-archive
        draft: false
        prerelease: false
        allowUpdates: true
        token: ${{ secrets.GITHUB_TOKEN }}
